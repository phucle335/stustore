# 2026-05-29 — Session log

**Topics:** CSS Modules migration (Motto + store + footer); mobile bottom navigation; checkout dark surface; product grid breakpoints; `MOTTO_NAV` sync

Full documentation: **[29-styling-css-modules.md](./29-styling-css-modules.md)** (includes **Mobile chrome** section).

## Summary

- Per-component CSS under `styles/components/{motto,store,home}/` with `import styles from "…module.css"` and `styles.camelCase` in TSX.
- Motto: `styles/motto/tokens.css` + `MottoHomePage` sets `motto-page` on `<html>`.
- Store: components import their own modules; `StoreShell` keeps `store-reset.css` + `store-alignment.css`.
- `SiteFooter` → `styles/components/home/SiteFooter.module.css`.
- **Mobile chrome:** bottom nav on Store (≤768px) and Motto (≤979px); menu drawer from left, search sheet from top, cart dark sidebar; shared `MobileOverlayLogoHeader`; account drawer from right (`AccountMenuDrawer`).
- **Checkout:** `CheckoutView.module.css` + `staticPageDark`; do not chain dark selectors across CSS module files.
- **Product grid:** 1 / 2 / 4 columns at 768px and 1024px in `ProductCatalog.module.css`.
- **MOTTO_NAV:** same links as `NAV_LINKS` (includes Trang chủ).

## For the next agent

1. Read [29-styling-css-modules.md](./29-styling-css-modules.md) before editing styles — especially **Mobile chrome** when touching `Header`, `MottoHeader`, `StoreShell`, or overlay components.
2. Append changes under **Changelog** in that file (with date) when you extend the system.
3. Do not reintroduce `store-header.css`-style global component CSS without a documented reason.
4. Overlay UX: only one of menu drawer / search sheet open at a time; reuse `MobileOverlayLogoHeader` for new mobile panels if they need the site logo.
